import { Modal } from "antd";
import React from "react";

interface SettingModalProps {
  open?: boolean;
}

const SettingModal: React.FC<SettingModalProps> = ({ open }) => {
  return <Modal title="ตั้งค่า">



  </Modal>;
};

export default SettingModal;
